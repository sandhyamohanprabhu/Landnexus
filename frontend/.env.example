# Live Backend URL for production (e.g., Render, Railway, Fly.io, or ngrok tunnel)
# Leave empty or omit during local development to use the local dev proxy (http://127.0.0.1:8001)
VITE_API_URL=https://your-backend-service.onrender.com
